\subsection{Two-dimensional case}
We are interested in estimating the exit time of a particle from a domain $D\subset\mathbb{R}^2$. Given $W(t)$ a two dimensional Brownian motion, a vector $\mu \in D$ and a scalar $\sigma \in \mathbb{R}$, we consider the following equation
\begin{equation}\label{eq:reference_equation}
	\begin{cases}
		dX(t) = -A(X(t)-\mu)dt + \sigma dW(t), \quad A\in \mathbb{R}^{2\times 2}, X(t) \in \mathbb{R}^2, 0\leq t\leq T,\\
		X(0) = X_0, \quad X_0 \in D,
	\end{cases}
\end{equation}
where we the matrix A is diagonal and is defined as
\begin{equation}\label{eq:diag_matrix}
A = \begin{pmatrix} \alpha & 0 \\
		0 & \beta \end{pmatrix}, \alpha,\beta \in \mathbb{R}.
\end{equation}
In this case, the two components of the solution $X(t)$ are independent, hence the analytical solution of \eqref{eq:reference_equation} is known and can be expressed componentwise as
\begin{align}\label{eq:exact_solution}
\begin{split}
	X_1(t) &= \mu_1 + e^{-\alpha t}(X_1(0) - \mu_1) + \int_0^t \sigma e^{-\alpha(t-s)}dW_1(s), \\
	X_2(t) &= \mu_2 + e^{-\beta t}(X_2(0) - \mu_2) + \int_0^t \sigma e^{-\beta(t-s)}dW_2(s).
\end{split}
\end{align}

\input{FirstEx/FirstEx_Num}


